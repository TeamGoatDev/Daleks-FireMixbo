from Enum import *

class ReturnCodes():
    INVALID_MOVE   = Enum.addId()
    INVALID_ACTION = Enum.addId()
    DEAD_DOCTOR    = Enum.addId()
    END_TURN       = Enum.addId()
    SUCCESS	       = Enum.addId()
